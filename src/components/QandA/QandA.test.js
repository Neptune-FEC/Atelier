// TESTING updateQsStateHelper
// updates state.currProduct with props.product after API call
// updates state.listOfQuestions with
  // API retrieved questions,
  // sorted by sorted helper func

// TESTING sorting qList by helpfulness rating
// input: unsorted list of Qs as a result of API/get request
// o: sorted list based on descending helpfulness values
  // first q in list should have highest rating
  // currQ rating > nextQ rating for all Qs in list
// c:
// e: for equal ratings, no preference which comes first
